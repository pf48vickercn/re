v0.`44` pwd
333  ls
git version
git config -global user.name "zhuyun"
git config -global user.email "781999207@qq.com"
git init 
ls -Force
git add 111.txt
git add .
git commit #先按i,输入备注，然后esc退出编辑，最后:wq
git log
git add .
git commit -m "第二次提交"
git commit -m "fix(111.txt):change content"
git checkout head 111.txt


…or create a new repository on the command line
echo "# re" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/pf48vickercn/re.git
git push -u origin main

…or push an existing repository from the command line
git remote add origin https://github.com/pf48vickercn/re.git
git branch -M main
git push -u origin main

git clone https://github.com/pf48vickercn/re01.git .
git remote -v
git remote add upstream https://github.com/midorg-com/re01.git
git checkout -b kwc
git add .
git commit -m "add(1721841005):zy"
git push -u origin kwc

如果冲突，需要更新最新的
git fetch upstream
git merge upstream/main
git push